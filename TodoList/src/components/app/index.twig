<div class="wrapper">
  <header class="header">
    <h1 class="logo">Melodo</h1>
  </header>

  <main>
    {% if isFilterTop %}
      {% mount '../filters' %}
    {% endif %}
    
    {% if isRenderBottom %}
      {% mount '../todoList' %}
      {% mount '../addTodo' %}
    {% else %}
      {% mount '../addTodo' %}
      {% mount '../todoList' %}
    {% endif %}

    {% if isFilterBottom %}
      {% mount '../filters' %}
    {% endif %}
  </main>
</div>